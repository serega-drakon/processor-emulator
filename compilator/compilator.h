#ifndef PROCESSOR_EMULATOR_COMPILATOR_H
#define PROCESSOR_EMULATOR_COMPILATOR_H

#include <stdio.h>
#include "../memory/memory.h"

int compileFile(FILE* input, Stack* output);

#endif //PROCESSOR_EMULATOR_COMPILATOR_H
